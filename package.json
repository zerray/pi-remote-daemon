{
  "name": "pi-remote-daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Authenticated remote daemon for Pi sessions.",
  "bin": {
    "pi-remote-daemon": "./src/cli-runner.cjs"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.74.0",
    "jiti": "^2.6.1",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "pi": {
    "extensions": [
      "./src/extension/index.ts"
    ]
  }
}
