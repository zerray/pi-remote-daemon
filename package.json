{
  "name": "pi-remote-control",
  "version": "1.0.6",
  "type": "module",
  "description": "Authenticated remote control for Pi sessions.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/zerray/pi-remote-control"
  },
  "keywords": [
    "pi-package"
  ],
  "bin": {
    "pi-remote-control": "./src/cli-runner.cjs",
    "pi-relay-push-gateway": "./src/push-gateway-runner.cjs"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.2",
    "@earendil-works/pi-coding-agent": ">=0.80.4 <0.85.0",
    "@types/qrcode-terminal": "^0.12.2",
    "jiti": "^2.6.1",
    "qrcode-terminal": "^0.12.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  },
  "peerDependencies": {
    "typebox": "*"
  },
  "pi": {
    "extensions": [
      "./src/extension/index.ts"
    ]
  }
}
